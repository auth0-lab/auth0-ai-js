{
  "name": "auth0-ai-js",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.4.2"
  },
  "packageManager": "npm@9.8.0",
  "workspaces": [
    "packages/*",
    "examples/authorization-for-rag/*"
  ],
  "dependencies": {
    "@openfga/sdk": "^0.8.0"
  }
}
