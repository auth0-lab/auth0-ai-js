import { describe, expect, it } from "vitest";

import { useAgentChatInterruptions } from "../../src/react/index";

describe("useAgentChatInterruptions", () => {
  it("should be defined", () => {
    expect(useAgentChatInterruptions).toBeDefined();
  });

  //todo add tests
});
